// Generated by CoffeeScript 1.7.1
(function() {
  define(['Tools/Tool', 'Tools/MoveTool', 'Tools/SelectTool', 'Tools/PathTool', 'Tools/ItemTool', 'Tools/LockTool', 'Tools/MediaTool', 'Tools/TextTool', 'Tools/ScreenshotTool', 'Tools/GradientTool'], function(Tool) {
    var ToolManager;
    ToolManager = (function() {
      function ToolManager() {
        var defaultFavoriteTools, error;
        R.toolsJ = $(".tool-list");
        R.favoriteToolsJ = $("#FavoriteTools .tool-list");
        R.allToolsContainerJ = $("#AllTools");
        R.allToolsJ = R.allToolsContainerJ.find(".all-tool-list");
        R.favoriteTools = [];
        if (typeof localStorage !== "undefined" && localStorage !== null) {
          try {
            R.favoriteTools = JSON.parse(localStorage.favorites);
          } catch (_error) {
            error = _error;
            console.log(error);
          }
        }
        Tool.car = new Tool.Car();
        Tool.gradient = new Tool.Gradient();
        Tool.lock = new Tool.Lock();
        Tool.media = new Tool.Media();
        Tool.move = new Tool.Move();
        Tool.screenshot = new Tool.Screenshot();
        Tool.select = new Tool.Select();
        Tool.text = new Tool.Text();
        defaultFavoriteTools = [];
        while (R.favoriteTools.length < 8 && defaultFavoriteTools.length > 0) {
          Utils.Array.pushIfAbsent(R.favoriteTools, defaultFavoriteTools.pop().label);
        }
        Tool.move.select();
        R.wacomPlugin = document.getElementById('wacomPlugin');
        if (R.wacomPlugin != null) {
          R.wacomPenAPI = wacomPlugin.penAPI;
          R.wacomTouchAPI = wacomPlugin.touchAPI;
          R.wacomPointerType = {
            0: 'Mouse',
            1: 'Pen',
            2: 'Puck',
            3: 'Eraser'
          };
        }
        return;
      }

      return ToolManager;

    })();
    return ToolManager;
  });

}).call(this);

//# sourceMappingURL=ToolManager.map

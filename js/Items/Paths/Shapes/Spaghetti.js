// Generated by CoffeeScript 1.7.1
(function() {
  var angle, bounds, center, circle, data, delta, i, newRadius, point, radius, scriptName, _i, _j;

  scriptName = "spaghetti";

  paper.project.clear();

  bounds = paper.view.bounds;

  center = bounds.center;

  data = [];

  for (i = _i = 0; _i <= 100; i = ++_i) {
    data.push(Math.random());
  }

  point = center;

  radius = Math.random() * 10;

  angle = Math.random() * 360;

  for (i = _j = 0; _j <= 100; i = ++_j) {
    circle = new paper.Path.Circle(point, radius);
    angle += Math.random() * 10;
    newRadius = Math.random() * 10;
    delta = new paper.Point({
      length: radius + newRadius,
      angle: angle
    });
    point = point.add(delta);
    radius = newRadius;
    circle.strokeColor = 'black';
    circle.strokeWidth = 1;
  }

}).call(this);
